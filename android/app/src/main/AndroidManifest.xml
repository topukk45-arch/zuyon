<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!--
      这里故意是空的。

      足用不声明任何权限,包括 INTERNET。装完在系统「权限」里一看就是空的 ——
      这是唯一一张用户能当场验证的牌。声明了再不用只是承诺,不声明是物理不可能。

      Capacitor 通过 WebViewAssetLoader 从 apk 内部读资源,走的是本地拦截,
      不需要网络权限。若日后加了需要联网的东西,请先想清楚要不要破这条。

      下面这行不是「声明」,是「删除」:Capacitor 的 AAR 自己带了 INTERNET,
      manifest 合并时会塞回来,tools:node="remove" 把它拆掉。
      CI 里会检查最终合并产物,确认这行真的生效了。
    -->
    <uses-permission android:name="android.permission.INTERNET" tools:node="remove" />
</manifest>
